# Signaling server (Node) — used by `npm run signaling`
CLOUDFLARE_TURN_TOKEN_ID = "yourId"
CLOUDFLARE_TURN_TOKEN_API = "yourApiKey"

# Client build (Vite) — WebSocket URL of the signaling server.
# Leave unset to default to ws://<hostname>:8080 in local development.
VITE_SIGNALING_URL = ""
